# GH-017: Present The Camera Mirror

# What

- Added `camera` as the third explicit coordinator channel and
  `Ctrl`+`Alt`+`3` as its fixed keyboard shortcut.
- Added a full-screen, horizontally mirrored, video-only Camera channel with a
  persistent local-only label and shared room feedback above it.
- Bound camera capture to the observed Camera component lifetime, including
  complete track release on exit, reload, ended streams, failed attachment, and
  stale asynchronous results.
- Added explicit unsupported, exact-origin policy, permission-denied,
  unavailable, and ended-stream presentation with retry only after reentry.
- Added fake-stream lifecycle tests and documented the third channel in the
  coordinator operator guide.
- Left the Chromium video policy and physical iMac confirmation pending because
  their accepted GH-014 dependency has not been implemented or merged.

# Acceptance Criteria

- [x] The coordinator accepts exactly `today`, `music`, and `camera` for
  `channel.select`; invalid values still fail without changing active state.
- [x] `Ctrl`+`Alt`+`3` submits Camera through the existing action lifecycle,
  while repeat or extra modifiers remain ignored.
- [x] Camera capture starts only after an observed `channel.active` snapshot
  reports `camera`, never merely because a request was submitted or accepted.
- [ ] The selected iMac camera appears mirrored, full-screen, correctly cropped,
  and without captured audio. The implementation and automated constraint and
  presentation checks are complete; the physical iMac result remains for the
  reviewer.
- [ ] Camera remains unmistakably labeled as a local mirror while active, and
  shared connection, lighting, identify, scene, and channel feedback remains
  visible above it. Structure and copy are automated; final visual judgment
  remains for the reviewer.
- [x] Selecting Today or Music stops every Camera `MediaStreamTrack` before the
  other channel is painted.
- [x] Cleanup, capture rejection, ended tracks, reload, and error paths release
  owned media and cannot attach a stale stream after reentry.
- [x] Unsupported API, missing camera, exact-origin policy failure, permission
  denial, and ended-stream states show clear local failure rather than a blank
  or frozen full-screen view.
- [x] Re-entering Camera makes one fresh bounded capture attempt and can recover
  after a transient failure.
- [ ] Chromium grants video capture only to the configured coordinator origin;
  no wildcard, global all-media flag, stored deployment hostname, or unrelated
  page receives permission. This waits for GH-014's shared policy.
- [x] No frame, image, device label, or stream metadata is posted to the
  coordinator, added to SSE, persisted, logged, or included in agent context.
- [x] Component lifecycle tests use owned fake streams to prove start, stop,
  stale-result, failure, ended-stream, reload, and reentry behavior without real
  camera hardware.
- [x] Biome, frontend tests, production build, production audit, Python suites,
  current endpoint provisioning syntax checks, and whitespace checks pass.

# Plan Diff

None. The endpoint policy and physical confirmation remain pending at the
dependency boundary required by the accepted plan.

# Implementation Walkthrough

`coordinator/cortex_home.py` now validates the fixed set `today`, `music`, and
`camera`. A successful Camera request follows the unchanged action sequence:
the coordinator publishes accepted action status, publishes
`channel.active: {"active": "camera"}`, and only then publishes completion.
Invalid values fail before the coordinator mutates its current channel.

`coordinator/client/src/room-state.js` maps only exact, non-repeated
`Ctrl`+`Alt`+`3` keydown events without Shift or Meta to the existing
`channel.select` action. `App.jsx` does not render Camera from that request or
its acknowledgement. It keeps rendering the old channel until the SSE reducer
receives the observed Camera snapshot, then uses the existing explicit channel
switch to mount `CameraChannel`.

`coordinator/client/src/CameraChannel.jsx` owns the visible Camera presentation.
Its video element is muted, inline, control-free, horizontally mirrored, and
uses cover cropping across the full display. The local mirror label remains
above the video while `RoomFeedback` stays outside the channel component at its
existing higher shared layers. Failure copy replaces video instead of leaving a
blank or frozen surface.

`coordinator/client/src/camera.js` owns one capture generation. It requests
exactly `{audio: false, video: true}`, validates that the returned stream has
video, and attaches only a still-current stream. Its cleanup detaches the
element and stops every returned track. A stale result stops itself. A track
ending releases all sibling tracks and shows the ended state. Page hide
performs the same cleanup for Chromium reload or exit. The component uses a
layout-effect cleanup so a transition away releases the Camera before the next
channel is painted. There is no automatic retry; remounting after deliberate
reentry creates the next single request.

`coordinator/client/src/camera.test.js`, `channels.test.js`,
`room-state.test.js`, and `coordinator/tests/test_coordinator.py` cover capture
ownership, stale resolution, ended streams, reload, reentry, explicit error
classification, mirror presentation, keyboard classification, and coordinator
state ordering. The implementation adds no canvas, recorder, upload, payload,
log, agent projection, or frontend dependency.

To reconstruct the current implementation and run it locally:

```sh
pnpm --dir coordinator/client install --frozen-lockfile
python3 coordinator/cortex_home.py --host 127.0.0.1
pnpm --dir coordinator/client dev
```

The coordinator and Vite commands run in separate terminals. Loopback is a
browser secure context, so a development machine with a camera can exercise
the local capture flow without the still-pending iMac HTTP-origin policy.

After GH-014 merges, fetch and rebase this branch before editing endpoint
provisioning. Extend GH-014's generated exact-origin Chromium policy with only
`VideoCaptureAllowedUrls` for the same configured origin. Do not add a second
policy file, wildcard, stored origin, or global media flag. The stable
deployment entry points will remain:

```sh
ssh imac 'sed -n "1p" /etc/cortex-endpoint/coordinator-url'
./coordinator/install <server-ssh-host>
./endpoint/imac/provision
```

The first command resolves the deployed exact origin from its source of truth;
use its complete output when endpoint provisioning asks for the coordinator
origin. The address is runtime configuration and must not be guessed or copied
into Git.

The final record must replace this dependency note with the exact policy path,
effective-policy inspection, and physical verification commands after that
integration exists.

# Problems Encountered

## 1. GH-014 policy boundary is not available

- Symptom: Neither the GH-014 worktree nor the remote contains implementation
  commits beyond the shared issue-planning baseline.
- Diagnosis: GH-017 is allowed to begin isolated component and channel work,
  but the accepted workflow explicitly prohibits it from creating a competing
  Chromium media policy before GH-014 merges.
- Resolution: Implemented and verified only the independent channel contract,
  local lifecycle, presentation, and tests. Fetched the remote and confirmed
  the dependency state before leaving endpoint provisioning unchanged.
- Remaining caveat: Exact-origin video permission, unattended prompt-free
  capture, endpoint reprovisioning, and physical iMac confirmation remain
  required after GH-014 merges.

## 2. Worktree-local frontend packages were absent

- Symptom: The initial frontend test command could not resolve React.
- Diagnosis: This issue worktree had the committed lockfile but no local
  `node_modules`.
- Resolution: Ran
  `pnpm --dir coordinator/client install --frozen-lockfile`; pnpm reused the
  locked package store without changing the manifest or lockfile.
- Remaining caveat: Dependencies remain uncommitted, worktree-local build
  inputs.

# Confirmation

## Automated Checks

- [x] `python3 -m unittest discover -s coordinator/tests` — 69 tests passed.
- [x] `python3 -m unittest discover -s endpoint/imac/tests` — 8 tests passed.
- [x] `python3 -m py_compile coordinator/cortex_home.py coordinator/context.py coordinator/hue.py coordinator/hue_pair.py coordinator/today.py coordinator/verify_scene.py`
- [x] `sh -n coordinator/install coordinator/install-host coordinator/pair-hue endpoint/imac/provision endpoint/imac/provision-host endpoint/imac/provision-raspotify endpoint/imac/provision-raspotify-host`
- [x] `pnpm --dir coordinator/client check`
- [x] `pnpm --dir coordinator/client test` — 23 tests passed.
- [x] `pnpm --dir coordinator/client build`
- [x] `pnpm --dir coordinator/client audit --prod` — no known
  vulnerabilities.
- [x] `git diff --check`

## Manual Testing

These checks require either a development machine camera or, after GH-014
integration, the shared deployed room. Physical iMac and privacy judgments
remain explicitly with the reviewer.

- [ ] Start the local feature with the two local-development commands in the
  walkthrough. Press `Ctrl`+`Alt`+`3`. The existing channel toast should appear
  first; only after the coordinator's Camera snapshot arrives should the page
  request camera permission and replace Today or Music.
- [ ] Allow camera access. A cropped edge-to-edge mirror should appear with
  left and right reversed, no browser controls, no captured audio, and a
  visible `Camera` / `Local mirror · Video stays on this iMac` label. Current
  lighting, connection, identify, scene, and channel feedback should remain
  readable above it.
- [ ] While Camera is live, press `Ctrl`+`Alt`+`1`, then reenter Camera and
  press `Ctrl`+`Alt`+`2`. Each transition should remove the browser camera-use
  indicator before the selected channel is visible. Each reentry should make
  one fresh request and restore a current moving image rather than a frozen
  previous stream.
- [ ] Press `Ctrl`+`Alt`+`3` while holding Shift or Meta, and send a repeated
  keydown. Camera should not be selected and no capture prompt or indicator
  should appear.
- [ ] Deny camera permission, then leave and reenter Camera. The denied state
  should remain visible without retrying in the background; reentry should make
  exactly one new attempt.
- [ ] Exercise unsupported or unavailable capture and an ended stream. The
  display should respectively show the supported explicit failure copy, never
  a blank or frozen view, while other room feedback remains available.
- [ ] After GH-014 integration, deploy with the two stable installer commands
  in the walkthrough, inspect Chromium's effective managed policy, and verify
  that secure-context, audio capture, and video capture all contain the same
  configured coordinator origin. No entry may contain `*`, another origin, or
  an all-media launch flag.
- [ ] Reboot the iMac and select Camera without interacting with Chromium. The
  built-in iSight should open without a prompt, remain correctly framed and
  mirrored at 1920×1200, release on exit, and recover after reentry and reboot.
  Confirm CPU, memory, temperature, and fan behavior remain acceptable for the
  room.
- [ ] While Camera is active, inspect coordinator requests, SSE, service logs,
  and agent context. No frame, image, device label, stream metadata, or Camera
  network payload should appear.

# Heatmap

Reference: `../project/HEATMAP.md`.

## Hot

### H1 - Let observed channel state authorize capture

- Decision: Mount capture only from the observed active Camera snapshot and
  release it synchronously on every transition away.
- Where: `App.jsx`, `CameraChannel.jsx`, and `camera.js`.
- Why: Request acceptance is not proof that the shared display changed, while
  camera lifetime must remain bounded to its visible channel.
- Alternatives: Start from keydown, start from action acknowledgement, or keep
  one background stream.

### H2 - Keep the mirror direct and local

- Decision: Attach the owned stream directly to one muted mirrored video
  element and expose no frame-processing boundary.
- Where: `CameraChannel.jsx` and `camera.js`.
- Why: Direct attachment provides the accepted local mirror without creating
  snapshots, recordings, coordinator payloads, or agent input.
- Alternatives: Canvas, recorder, endpoint streaming service, or retained
  background media.

## Warm

- `camera.js`: Classifies browser capture failures into the fixed presentation
  states and treats deliberate reentry as the only retry.
- `CameraChannel.jsx`: Presents current Camera state while retaining shared room
  feedback layering.

## Cold

| Where | What |
|---|---|
| `coordinator/cortex_home.py` | Adds the third value to fixed channel validation. |
| `coordinator/client/src/room-state.js` | Adds the third fixed keyboard mapping. |
| `coordinator/client/src/App.jsx` | Adds Camera to the explicit channel switch. |
| `coordinator/README.md` | Documents the existing action with its third value and shortcut. |

## Stylistic

### S1 - Make the preview feel like a mirror

- Choice: Fill the display with horizontally mirrored cover video and retain
  only a restrained Camera/local-only label over the live image.
- Alternative: Letterboxed player, dashboard card, controls, or unmirrored
  capture.
- When to apply: Live Camera presentation only; failure states use the
  established full-room typography and feedback language.

# Notes

- Camera frames and metadata never enter coordinator state. Agent context was
  intentionally not extended in this issue; its behavior while Camera is
  active remains deferred as accepted in the plan.
- Do not deploy the Camera channel to the HTTP-origin kiosk as complete until
  GH-014 has merged and its exact-origin media policy has been rebased and
  extended.
