# GH-001: Bootstrap The Cortex Home Repository

# What

- Established the accepted Cortex Home idea, roadmap, and first Planpoint as the
  repository's planning baseline.
- Replaced the copied Crossroads project entry point and issue record with
  Cortex Home-specific documentation.
- Renamed the local folder and empty GitHub repository to `cortex-home`.
- Recorded the qualified Ubuntu installation and preliminary iMac hardware
  baseline without storing machine or network identifiers.

# Acceptance Criteria

- [x] This folder is a Git repository with `main` as its default branch.
- [x] Accepted project planning is captured as the initial baseline.
- [x] Issue work occurs on a correctly named `GH-001` branch.
- [x] `README.md` introduces Cortex Home and links to the idea, roadmap, and
  current Planpoint.
- [x] The copied Crossroads issue record is replaced with this Cortex Home
  record.
- [x] The folder and empty GitHub repository use the `cortex-home` slug.
- [x] No application stack, CI system, child repository, or speculative ignore
  rules were introduced.

# Plan Diff

- The iMac was installed and inspected before GH-002. Its observed facts were
  added to the planning baseline, while remaining qualification work stays in
  GH-002.
- An existing empty public GitHub repository was discovered and renamed from
  `abode` to `cortex-home`. Publishing project history remains deferred.

# Confirmation

## Automated Checks

- Command: Search active product documentation case-insensitively for the
  previous project name, excluding this migration record and its plan.
- Result: Passed with no active-identity matches.
- Command: Verify each local Markdown target linked from `README.md` exists.
- Result: Passed.
- Command: Check repository history and branch placement.
- Result: Passed; planning precedes issue work and GH-001 uses its issue branch.
- Command: Inspect `FelixFolkebrant/cortex-home` through GitHub.
- Result: Passed; the public remote has the correct slug and remains empty.

## Manual Testing

- Start: Open `README.md` in a rendered Markdown view.
- Action: Read the introduction and current hardware summary.
- Expected result: Cortex Home's purpose and the ThinkPad, iMac, Sonos, and Hue
  roles are clear without duplicating the detailed project documents.
- Action: Open each document in the project-document table.
- Expected result: Every link opens the described Cortex Home document.

# Heatmap

Reference: `../project/HEATMAP.md`.

## Stylistic

### S1 - Keep The README As An Entry Point

- Choice: Introduce Cortex Home briefly, then link to the accepted Crossroads
  documents.
- Alternative: Duplicate the complete idea and roadmap in the README.
- When to apply: Keep durable project detail in its canonical document rather
  than maintaining it in two places.

# Notes

- The remote repository is intentionally empty until the local bootstrap
  history is accepted for publication.
- GH-002 continues iMac qualification, including audio routing, sustained
  temperature measurements, and the missing CPU, memory, and storage inventory.
